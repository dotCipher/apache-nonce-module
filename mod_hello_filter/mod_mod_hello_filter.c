/* 
**  mod_mod_hello_filter.c -- Apache sample mod_hello_filter module
**  [Autogenerated via ``apxs -n mod_hello_filter -g'']
**
**  To play with this sample module first compile it into a
**  DSO file and install it into Apache's modules directory 
**  by running:
**
**    $ apxs -c -i mod_mod_hello_filter.c
**
**  Then activate it in Apache's httpd.conf file for instance
**  for the URL /mod_hello_filter in as follows:
**
**    #   httpd.conf
**    LoadModule mod_hello_filter_module modules/mod_mod_hello_filter.so
**    <Location /mod_hello_filter>
**    SetHandler mod_hello_filter
**    </Location>
**
**  Then after restarting Apache via
**
**    $ apachectl restart
**
**  you immediately can request the URL /mod_hello_filter and watch for the
**  output of this module. This can be achieved for instance via:
**
**    $ lynx -mime_header http://localhost/mod_hello_filter 
**
**  The output should be similar to the following one:
**
**    HTTP/1.1 200 OK
**    Date: Tue, 31 Mar 1998 14:42:22 GMT
**    Server: Apache/1.3.4 (Unix)
**    Connection: close
**    Content-Type: text/html
**  
**    The sample page from mod_mod_hello_filter.c
*/ 

#include "httpd.h"
#include "http_config.h"
#include "http_protocol.h"
#include "ap_config.h"

/* The sample content handler */
static int mod_hello_filter_handler(request_rec *r)
{
    if (strcmp(r->handler, "mod_hello_filter")) {
        return DECLINED;
    }
    r->content_type = "text/html";      

    if (!r->header_only)
        ap_rputs("The sample page from mod_mod_hello_filter.c\n", r);
    return OK;
}

static void mod_hello_filter_register_hooks(apr_pool_t *p)
{
    ap_hook_handler(mod_hello_filter_handler, NULL, NULL, APR_HOOK_MIDDLE);
}

/* Dispatch list for API hooks */
module AP_MODULE_DECLARE_DATA mod_hello_filter_module = {
    STANDARD20_MODULE_STUFF, 
    NULL,                  /* create per-dir    config structures */
    NULL,                  /* merge  per-dir    config structures */
    NULL,                  /* create per-server config structures */
    NULL,                  /* merge  per-server config structures */
    NULL,                  /* table of config file commands       */
    mod_hello_filter_register_hooks  /* register hooks                      */
};

